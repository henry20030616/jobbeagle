---
description: Agent 代跑 GitHub / Supabase / Vercel 整合與部署；使用者只做瀏覽器 OAuth
alwaysApply: true
---

# Agent 代跑運維（GitHub · Supabase · Vercel）

## 原則

- **能代跑的一律由 Agent 執行**（Shell、腳本、API、git push），不叫使用者開終端機。
- 使用者**只做** Agent 無法代做的 **瀏覽器一次性 OAuth / 第三方後台點擊**（一次列完）。
- 一鍵代跑：`bash scripts/ops/sync-all.sh`（Vercel env 同步 → 整合驗證 → Production redeploy）。

## GitHub — Agent 代跑

| 動作 | 命令 / 方式 |
|------|-------------|
| 確認 remote | `git remote -v` → `github.com/henry20030616/jobbeagle` |
| 拉取 / 比對 | `git fetch origin`、`git status` |
| 提交並推送 | `git add/commit/push origin main`（觸發 Vercel 自動部署） |
| CI | push 後由 `.github/workflows` 自動跑 |

**使用者僅需：** 首次若 push 要授權，在 Cursor 彈窗點 Allow；無需手動開 GitHub 設定頁。

## Supabase — Agent 代跑

| 動作 | 命令 / 方式 |
|------|-------------|
| 驗證 DB | `node scripts/ops/verify-supabase.mjs` |
| 驗證表 | `node scripts/ops/verify-integrations.mjs`（profiles / referrals / analysis_reports） |
| 同步 env 到 Vercel | `scripts/ops/sync-vercel-env.sh`（`NEXT_PUBLIC_SUPABASE_*`、`SUPABASE_SERVICE_ROLE_KEY`） |
| CLI migration | `npx supabase link --project-ref yvzorfeespljbitxxufo` + `db push`（需先 `supabase login`） |

**專案 ref：** `yvzorfeespljbitxxufo`

**使用者僅需：**
1. **`supabase login`** 首次瀏覽器授權（Agent 發連結，使用者點完說「好了」）
2. **Dashboard 貼 OAuth**：Authentication → Providers → GitHub / Google（`.env.local` 的 `GITHUB_*` 給 Supabase 後台，不進 Vercel）
3. **SQL Editor** 僅當無 `DATABASE_URL` 且 CLI 無法 push migration

## Vercel — Agent 代跑

| 動作 | 命令 / 方式 |
|------|-------------|
| 登入狀態 | `npx vercel whoami` |
| Link 專案 | `npx vercel link --yes` |
| 同步 env | `scripts/ops/sync-vercel-env.sh` |
| Production 部署 | `npx vercel deploy --prod --yes` |
| 驗證線上 | `curl -sI https://www.jobbeagle.com` |

**使用者僅需：** 首次 `vercel login` / `supabase login` 的 device OAuth 連結（各一次）。

## 其他代跑

- Lemon Squeezy env 同步（`.env.local` 有值時由 sync 腳本帶上）
- `curl` 驗收 `/api/checkout`、`/api/profile`
- `npm run build` 本機 build 檢查

**勿 commit / 勿在對話貼出** `.env.local` 內 secret。

## 結束交付

1. 已完成項（GitHub / Supabase / Vercel 各一行）
2. 若仍缺：僅列瀏覽器要點的 1–3 件事 + 直連
3. 卡關：請使用者貼完整錯誤

## 與其他 rule

- 程式變更後 push：見 `git-github-vercel-always.mdc`
- commit 以使用者當次明確指示為準；預設 Agent 提交並 push
