/**
 * @deprecated Import from `@/lib/rate-limit` instead.
 * Re-exports kept so existing analyze imports keep working.
 */
export {
  rateLimitAnalyze,
  rateLimit,
  rateLimitExtensionCapture,
  clientIpFromRequest,
} from '@/lib/rate-limit';
